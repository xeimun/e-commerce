# AGENTS.md

## 1. 프로젝트 목적

- CS 지식을 활용해 성능 문제를 분석하고 개선하는 이커머스 백엔드 프로젝트를 만든다.
- 기능 구현, 성능 측정, 병목 분석, 개선, 재측정을 통해 정량적인 개선 결과를 남긴다.
- 구현과 문서화는 포트폴리오와 면접에서 설명 가능한 수준으로 정리한다.
- 기술을 먼저 끼워 맞추지 않고, 기능 구현과 측정 결과를 바탕으로 필요한 개선 방향을 선택한다.

## 2. 학습 컨텍스트

- 이 프로젝트는 백엔드와 CS 개념을 구현 중심으로 학습하며 진행한다.
- Codex는 프로젝트의 학습 흐름을 고려해 필요한 개념을 짧게 설명하고, 사용자가 추가 질문하면 더 깊게 설명한다.

## 3. 작업 시작 원칙

- 사용자가 "만들어", "구현해", "작성해"라고 명시하기 전에는 파일 생성, 코드 작성, 설정 변경을 하지 않는다.
- Codex가 구현 준비가 되었다고 판단하더라도, 먼저 사용자에게 구현해도 되는지 확인한다.
- 요구사항이 애매하거나 비즈니스 규칙에 영향을 주는 경우 구현 전에 사용자에게 질문한다.
- 파일 생성 또는 큰 수정 전에는 생성/수정 대상과 목적을 먼저 요약한다.

## 4. 설명과 학습 원칙

- 설명은 구현에 필요한 개념 중심으로 짧게 제공한다.
- 사용자가 추가 질문하면 더 깊게 설명한다.
- 새로운 CS/백엔드 개념을 실제 구현에 적용했거나, 면접 답변에 도움이 될 만한 개념이라고 판단될 때 `docs/learning-notes.md`에 기록한다.
- 학습 문서는 모든 대화 내용을 기록하지 않는다. 프로젝트 적용 지점과 학습 가치가 있는 내용을 이해하기 쉽게 요약한다.

## 5. 문서화 원칙

- 문서는 한국어로 작성한다.
- 문서는 목적에 맞게 상세도를 조절한다.
- `README.md`는 외부 공개용 요약 중심으로 작성한다.
- `docs/requirements.md`는 구현 기준이 되도록 명확하고 검증 가능한 규칙으로 작성한다.
- `docs/domain-model.md`는 핵심 도메인 개념과 책임을 정리한다.
- `docs/api-spec.md`는 API 요청, 응답, 에러 형식을 정리한다.
- `docs/decision-log.md`는 면접 답변에 활용할 수 있도록 중요한 선택의 배경과 대안을 함께 기록한다.
- `docs/learning-notes.md`는 실제 구현에 연결되는 개념을 이해하기 쉽게 정리한다.
- `docs/performance-report.md`는 성능 측정, 병목 가설, 개선 내용, 재측정 결과를 정리한다.
- 요구사항 변경은 `docs/requirements.md`에 반영한다.
- 도메인 변경은 `docs/domain-model.md`에 반영한다.
- API 변경은 `docs/api-spec.md`에 반영한다.
- 중요한 기술 선택은 `docs/decision-log.md`에 기록한다.
- 학습 내용은 `docs/learning-notes.md`에 기록한다.
- 성능 측정과 개선 결과는 `docs/performance-report.md`에 기록하고, `README.md`에는 요약만 반영한다.
- 기능 구현이 완료되면 다음 기능 구현 때 참고할 수 있도록 관련 문서를 함께 갱신한다.
- 구현 변경에 따라 요구사항, 도메인, API, 결정, 학습, 성능 문서 중 관련 문서를 선택해 반영한다.
- 문서 파일명을 변경하면 이 문서의 경로도 함께 갱신한다.

## 6. 보안과 민감정보 원칙

- 문서와 코드에는 실제 비밀번호, 토큰, API 키, 개인 로컬 절대 경로, 운영 DB 접속 정보 등 민감정보를 기록하지 않는다.
- 예시 값이 필요하면 실제 값이 아닌 더미값을 사용한다.
- 실제 실행 값은 환경 변수, `.env`, 또는 Git에 포함되지 않는 로컬 설정 파일로 관리한다.
- 민감정보가 포함될 가능성이 있는 변경은 커밋 또는 push 전에 다시 확인한다.

## 7. 디자인 참조 원칙

- 프론트엔드 또는 UI 관련 작업 전에는 `DESIGN.md`를 먼저 확인한다.
- `DESIGN.md`는 시각 디자인, 컴포넌트 스타일, 반응형, 아이콘 사용 기준으로 활용한다.
- `AGENTS.md`는 작업 방식과 개발 원칙을 정의하고, `DESIGN.md`는 UI 디자인 기준을 정의한다.
- 두 문서가 충돌하면 작업 방식과 개발 원칙은 `AGENTS.md`, 시각 디자인은 `DESIGN.md`를 우선한다.
- UI 구현 중 `DESIGN.md`의 기준과 다르게 가야 할 경우, 이유를 설명하고 사용자 확인을 받는다.

## 8. 기술 스택

- Java 17
- Spring Boot 3.5.x
- Gradle
- Spring Web
- Spring Data JPA + Hibernate
- PostgreSQL
- Flyway
- JUnit 5
- IntelliJ IDEA Ultimate
- Docker Compose는 PostgreSQL 실행용으로만 사용한다.
- Swagger/OpenAPI는 주문 API 구현 이후 도입한다.
- Redis, Kafka, MSA는 초기 구현에 도입하지 않는다. 기능 구현과 성능 측정 이후 필요성이 확인되면 단계적으로 검토한다.
- 프론트엔드 기술 스택은 아직 확정하지 않는다. 백엔드 핵심 기능 구현 후 인터뷰를 통해 결정한다.

## 9. 아키텍처와 코드 구조

- 초기 구현은 운영 가능한 단일 모듈 모놀리스 수준을 목표로 한다.
- 처음부터 과한 구조를 적용하지 않는다.
- 처음에는 단순 계층형 구조로 구현하고, 복잡해지면 도메인 중심 구조로 정리한다.
- 패키지는 기능 기준으로 나누고, 각 기능 내부는 계층형으로 구성한다.
- 예시 패키지 구조:

```text
order/
  controller/
  service/
  repository/
  entity/
  dto/

product/
  controller/
  service/
  repository/
  entity/
  dto/
```

- Entity와 DTO는 분리한다.
- API 응답으로 Entity를 직접 노출하지 않는다.
- 공통 에러 응답을 사용한다.

## 10. DB와 설정 관리

- DB는 PostgreSQL을 사용한다.
- DB는 Docker Compose로 실행한다.
- 애플리케이션은 IntelliJ IDEA Ultimate에서 실행한다.
- DB 스키마 변경은 Flyway로 관리한다.
- JPA `ddl-auto`는 `validate`를 사용한다.
- 테이블 생성, 컬럼 변경, 인덱스 추가는 Flyway 마이그레이션 SQL로 관리한다.

## 11. 테스트와 검증

- 테스트 전략은 기능마다 Codex가 추천하고 사용자와 함께 결정한다.
- 핵심 비즈니스 규칙은 단위 테스트를 우선 검토한다.
- DB 트랜잭션과 재고 차감은 통합 테스트로 검증한다.
- API 요청/응답은 필요한 경우 통합 테스트로 검증한다.
- 성능 테스트는 기능 완성 후 별도 단계에서 진행한다.
- 커밋 전 가능한 범위에서 테스트와 빌드를 실행한다.
- Windows 기준 Gradle Wrapper 명령어를 사용한다.

```powershell
.\gradlew.bat test
.\gradlew.bat build
```

## 12. 성능 개선 원칙

- 개선 방법은 기준 성능 측정과 병목 원인을 바탕으로 선택한다.
- 성능 개선은 다음 순서로 진행한다.

```text
기준 성능 측정 -> 병목 가설 -> 개선 적용 -> 재측정 -> 문서화
```

- 개선 전후 수치를 기록해 포트폴리오와 이력서에 활용한다.
- 성능 개선 과정에서는 처리량, 응답 시간, 실패율, DB 쿼리, 락 대기 등 측정 가능한 지표를 우선한다.
- 성능 측정과 개선 과정은 `docs/performance-report.md`에 기록한다.

## 13. 기능 구현 워크플로우 원칙

- 기능 구현으로 판단되는 작업을 실행할 때는 `feature-flow` skill을 사용한다.

## 14. Git과 PR 원칙

- `main`은 안정 브랜치로 사용한다.
- 구현 전 기능 브랜치를 생성한다.
- 기능 브랜치는 `feat/order-create`처럼 케밥 케이스를 사용한다.
- 문서 전용 작업이 필요하면 `docs/...` 브랜치를 사용할 수 있다.
- 기능 개발 중 발생한 문서 변경은 같은 기능 브랜치에서 함께 수정한다.
- 기능 하나가 끝날 때마다 커밋한다.
- 커밋 메시지는 Conventional Commits를 사용한다.
- 필요한 타입만 사용한다.

```text
feat
fix
docs
style
refactor
test
chore
comment
init
rename
remove
design
```

- 커밋 메시지 예시:

```text
docs: AGENTS 작업 규칙 추가
feat: 주문 생성 기능 추가
test: 주문 생성 서비스 테스트 추가
```

- push, PR 생성, 리뷰 흐름을 실무 연습으로 진행한다.
- 리뷰는 학습 흐름을 해치지 않는 선에서 핵심 문제 중심으로 진행한다.
- PR merge 여부는 사용자가 GitHub에서 최종 판단한다.
- 사용자가 PR을 merge했다고 알리면 Codex는 로컬 `main`을 원격과 동기화한다.
- PR merge 후 로컬 동기화는 `git fetch origin --prune`, `git switch main`, `git pull --ff-only origin main` 순서로 진행한다.
- `git pull --ff-only`가 실패하면 로컬 `main`에 원격과 다른 변경이 있다는 뜻이므로, Codex는 임의로 merge commit을 만들지 않고 상황을 보고한 뒤 rebase, stash, 별도 브랜치 분리 중 적절한 방법을 제안한다.
- PR merge가 완료된 작업 브랜치는 보통 삭제한다. 원격 브랜치는 GitHub의 Delete branch 또는 사용자의 요청에 따른 Codex 작업으로 정리하고, 로컬 브랜치는 `git branch -d <branch>`로 안전하게 삭제한다.
- PR 없이 `main`에 직접 반영하는 작업은 사용자가 명시적으로 요청한 간단한 문서 수정에 한정한다. 이 경우 Codex는 `main`이 최신인지 확인한 뒤 커밋하고 `origin/main`에 push한다.
- 인증이나 권한 문제로 push 또는 PR 생성이 막히면 사용자에게 보고하고 다음 행동을 확인한다.

## 15. PR 템플릿 원칙

- PR 템플릿은 `.github/pull_request_template.md`의 간단한 공통 양식을 사용한다.
- PR에서 가장 먼저 보이는 항목은 주요 작업으로 둔다.
- 검증은 테스트와 빌드 통과 여부를 체크박스로 표시한다.
- 문서 변경은 별도 고정 항목으로 분리하지 않고 주요 작업에 함께 적는다.

## 16. 작업 보고 원칙

- 작업 완료 보고는 상황에 맞게 하되, 변경 내용과 검증 결과를 포함한다.
- 테스트나 빌드를 실행하지 못한 경우 그 이유를 명확히 보고한다.
- 사용자가 명령한 범위를 넘어서는 작업은 제안만 하고 임의로 진행하지 않는다.
